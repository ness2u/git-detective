# Backlog (git-detective)

Planned / future improvements

- Suppress empty-repo errors: skip log when zero commits
- Optional: --match=exact|prefix|glob for AOI matching
- Optional: --paths FILE to accept plain text path list
- Output: concise per-repo summary header, counts
- Config: allow multiple keys/groups with labels
- Tests: add a tiny fixture repo and Bats tests in CI
- Performance: skip large directories by glob or depth limit

Release hygiene
- Keep CHANGELOG.md updated
- Version bump and tag on release
